```mermaid
flowchart TB
    subgraph Client["👤 ユーザ（ブラウザ）"]
        Browser["Vue 3 (Vite)"]
    end

    subgraph Frontend["🌐 フロントエンド"]
        S3["Amazon S3(静的ホスティング)"]
        CF["CloudFront(CDN)"]
    end

    subgraph Backend["⚙️ バックエンド"]
        APIGW["API Gateway"]
        Lambda["AWS Lambda(Spring Boot)"]
        ParamStore["SSM パラメータストア"]
        RDS["RDS (MySQL)Free Tier"]
    end

    subgraph DevOps["🔧 CI/CD"]
        GitHub["GitHub Actions"]
    end

    %% 接続関係
    Browser --> CF --> S3
    Browser --> APIGW

    APIGW --> Lambda
    Lambda --> ParamStore
    Lambda --> RDS

    GitHub --> S3
    GitHub --> Lambda
